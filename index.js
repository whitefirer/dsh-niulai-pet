/**
 * dsh-niulai-pet — host 侧空插件。
 *
 * 本插件全部能力在 client 半（lib/client.js，桌宠浮层纯浏览器实现）。
 * bundle 声明 + 本文件仅为满足 `dsh plugin add` 的安装识别
 * （awesome-dsh-plugin 收录要求 dsh.bundle manifest）。
 * @module dsh-niulai-pet
 */

/** cordis host 插件入口：无事可做， client 半由 web 运行时挂载。 */
export function apply() {}
